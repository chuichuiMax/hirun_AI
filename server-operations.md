# 本地服务器操作流程

本文件约束本项目的 SSH、Docker、远程服务器以及测试/生产环境实际操作。默认目标是本机 Docker Compose 开发环境；未经用户明确授权，不操作远程、测试或生产环境。

## 1. 授权与范围

- 执行启动、停止、重启、重建、发布、清理、删除或系统配置变更前，必须确认用户已明确授权本次操作。
- 将授权限定到用户指定的环境和服务；未点名时采用满足目标的最小服务集合，不默认重启数据库、对象存储、缓存、图数据库或向量数据库。
- 本地开发环境默认使用仓库根目录的 `docker-compose.yml`。构建默认在本地或 CI 完成；远程构建必须另行获得明确授权和风险确认。
- 不输出 `.env`、令牌、密码、私钥或完整环境变量。

## 2. 操作前检查

每次实际变更前完成并记录以下只读检查：

1. 确认当前目录、适用的项目指令和 Compose 文件。
2. 检查 `git status --short`，保留用户已有改动，不使用 `git reset --hard`、`git checkout --` 等破坏性命令。
3. 检查目标服务状态、容器名称、健康状态及最近错误日志。
4. 检查主机磁盘和内存。磁盘可用空间或可用内存低于总量的 10% 时，不执行启动、重启、构建或发布；只报告资源阻碍。
5. 检查 Compose 配置可解析；如配置校验失败，不继续变更。
6. 记录目标容器当前镜像 ID、启动时间和状态，便于故障定位。

## 3. 本地开发环境重启

- 优先使用 `docker compose restart <services>`，避免不必要的重建、拉取镜像或数据服务中断。
- 只重启实现目标所需的服务。应用代码变更通常限定为 `api`、`worker`、`web`；小红书浏览器链路变更可增加 `xhs-browser-gateway`。
- 不重启 `postgres`、`minio`、`redis`、`graph`、`milvus`、`milvus-etcd-dev` 等持久化服务，除非用户明确点名且已评估数据风险。
- 不在重启命令中使用 `--build`、`--force-recreate`、`down -v` 或清理参数，除非用户另行明确授权。

## 4. 发布与验证

操作后按以下顺序验证：

1. 等待目标容器进入运行或健康状态；单次等待不超过 60 秒。
2. 检查目标服务最近日志，确认无启动异常、迁移失败、循环重启或依赖连接错误。
3. 请求应用健康接口；涉及特定功能时，再执行最小真实链路验证。
4. 变更了代码时，运行与风险相称的测试、Lint 和构建检查；不得把未执行的检查描述为已通过。
5. 汇报实际重启的服务、健康检查、功能验证及仍存在的限制。

## 5. 回滚

- `docker compose restart` 不改变镜像、挂载或数据，因此重启失败时先检查日志和配置，不清理数据卷、不删除容器。
- 如果失败由本次源代码改动引起，先报告具体失败和建议回滚范围；未经用户明确授权，不覆盖、丢弃或回退工作区改动。
- 如果失败由 Compose 或镜像更新引起，使用操作前记录的镜像 ID 或原配置制定回滚命令，并在执行前再次确认目标明确且不触及数据卷。
- 回滚后重复健康检查和最小功能验证。

## 6. 事故熔断

出现以下任一情况时立即停止后续变更，保留容器、日志和数据现场并向用户报告：

- 磁盘或内存可用比例低于 10%。
- 数据库、对象存储或缓存出现数据损坏、迁移失败或不可恢复错误迹象。
- 目标服务连续两次启动或健康检查失败。
- 出现循环重启、异常资源增长、端口冲突或影响范围超出已授权服务。
- 需要删除容器、数据卷、持久化目录、数据库记录或修改系统配置才能继续。
- 无法确认目标环境、目标服务或回滚路径。

熔断后不得通过反复重启、清理数据或扩大操作范围尝试“碰运气”。必须先说明现状、影响、已完成检查和需要的下一项授权。
